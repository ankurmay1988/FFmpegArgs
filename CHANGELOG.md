# Changelog

All notable changes to this project are documented here.
This file is generated by [git-cliff](https://git-cliff.org) from Conventional Commits.

Each section is a **minor line** (tag `vM.m.0` opens the `M.m.x` line; commits after it are
that line's versions `M.m.<commits-since-tag>`). Within a line, commits are a **timeline**:
newest on top, oldest at the bottom, each prefixed with its conventional type. `— current`
marks the line still in progress.

## [2.3.x] — current

- *(docs)* Note ffmpeg n8.0.1 as generated/tested version
- *(build)* **(autogen)** Sync generated code + .other dumps to ffmpeg n8.0.1
- *(docs)* Mark demuxer/muxer consolidation as done
- *(refactor)* Route -re/image2/movflags through generic+typed API
- *(feat)* **(muxers)** Add MovMuxer + Mp4Muxer over shared BaseMp4Muxer
- *(feat)* **(demuxers)** Add typed Image2Demuxer (Image2Demux)
- *(docs)* Sync summary/roadmap with mux-demux merge + consolidation plan
- *(refactor)* **(demuxers)** Rename *Demuxer extension methods to *Demux
- *(fix)* **(muxers)** Correct BaseMuxer namespace to FFmpegArgs.Outputs.Muxers
- *(build)* **(symbol)** Drop loose .pdb pack line from merged Demuxers/Muxers nuspecs
- *(docs)* Drop v2.4.0/v2.5.0 tag references; note Turkish-i culture fix
- *(fix)* **(apulsator)** Timing() takes ApulsatorTiming (bpm/ms/hz), not ApulsatorMode
- *(fix)* **(culture)** ToUpperInvariant in Autogens identifier helper (Turkish-i at gen-time)
- *(fix)* **(culture)** Filter enums use NameAttribute instead of ToString().ToLower() (Turkish-i)
- *(docs)* Mark P1/P2/quality roadmap items done; note v2.4-v2.5 additions
- *(docs)* Add EXAMPLES.md (feature-grouped examples) and link from README
- *(fix)* **(culture)** Format numeric options with InvariantCulture
- *(feat)* **(subtitle)** Add -c:s codec selection and -sub_charenc input option
- *(feat)* **(options)** Add common muxer/demuxer option extensions (movflags, -re, image2)
- *(feat)* **(codec)** Add audio encoder wrappers (aac, libmp3lame, ac3, eac3, flac, alac, libopus, libvorbis)
- *(feat)* **(ffplay)** Add FFplayRender execute support (run ffplay from FFplayArg)
- *(test)* **(execute)** Verify CancellationToken kills ffmpeg promptly
- *(feat)* **(input)** Reactivate FilterStringInput for lavfi (-f lavfi -i)
- *(build)* **(symbol)** Nhung PDB + source vao DLL, bo pack .pdb roi
- *(docs)* **(roadmap)** Gop toan bo P3 vao P0, uu tien Symbol package lam truoc
- *(docs)* Note AutoSinkUnusedMapOut in codebase summary
- *(test)* Add auto-sink tests (arg-build + render)
- *(feat)* Auto-sink unused filter map outs (opt-in)
- *(docs)* Mark P1 sink filters done in roadmap and summary
- *(test)* Add no-ffmpeg arg-build tests for sink filters

## [2.2.x]

- *(feat)* Implement video/audio sink filters
- *(docs)* Mark P0 ffplay fix and escaping review done
- *(docs)* Keep FiltergraphEscapingLv3 stub as reserved
- *(fix)* Add missing dash to ffplay -filter_complex_script
- *(test)* Annotate RenderProgress as nullable in RenderProgressTest
- *(docs)* Note FFmpegArgTest moved to the render test project
- *(test)* Move FFmpegArgTest to FFmpegArgs.Test.Render and drop unused Render.cs
- *(docs)* Update roadmap and codebase summary for the test split + CI
- *(ci)* Add GitHub Actions workflow for the no-ffmpeg unit tests
- *(test)* Split into FFmpegArgs.Test (no ffmpeg) and FFmpegArgs.Test.Render
- *(docs)* Suffix Vietnamese docs with -vi
- *(docs)* Add codebase summary and roadmap
- *(build)* Pack via dotnet pack, restrict NuGet build to master
- *(build)* Version packages with GitVersion (vM.m.0 tags)
- *(build)* Suppress CS1591 (missing XML doc comment)

## [2.1.x]


<!-- generated by git-cliff -->
